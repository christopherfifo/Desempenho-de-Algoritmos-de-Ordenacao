#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "algoOrdena.h"
#include "algoritmosOrdenacao.h"

int main()
{
    srand(time(NULL));
    menuquantidade();

    menuprincipal();
    system("pause");
    exit(0);
    return 0;
}
